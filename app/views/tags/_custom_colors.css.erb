<% if @tags %>
  <% @tags.each_with_index do |tag,index| %>
    #tag-<%= tag.id %>{
      background-color: <%= tag.background_color %>;
      color: <%= tag.text_color %>;
    }

    #tag-<%= tag.id %> a{
      color: <%= tag.text_color %>;
    }
  <% end %>
<% end %>